---
/**
 * Docs layout — sidebar + content area + TOC.
 * Language switching handled by the header LanguagePicker, which reads
 * available locales from the #docs-locales JSON script tag.
 */
import Base from './Base.astro';
import DocsSidebar from '../components/DocsSidebar.astro';
import TOC from '../components/TOC.astro';
import Mermaid from '../components/Mermaid.astro';

interface Heading {
  depth: number;
  slug: string;
  text: string;
}

interface NavItem {
  title: string;
  slug: string;
  order: number;
}

interface NavSection {
  label: string;
  items: NavItem[];
}

interface LocaleOption {
  code: string;
  label: string;
  href: string;
}

interface Props {
  title: string;
  description?: string;
  headings: Heading[];
  sections: NavSection[];
  currentSlug: string;
  locale?: string;
  availableLocales?: LocaleOption[];
  mdPath?: string;
}

const { title, description, headings, sections, currentSlug, locale = 'en', availableLocales = [], mdPath } = Astro.props;
const dir = locale === 'fa' ? 'rtl' : 'ltr';
---

<Base title={`${title} — Meridian Docs`} description={description}>
  {mdPath && <link slot="head" rel="alternate" type="text/markdown" href={mdPath} />}
  {availableLocales.map((l) => (
    <link slot="head" rel="alternate" hreflang={l.code === 'zh' ? 'zh-Hans' : l.code} href={l.href} />
  ))}
  <div class="docs-layout" dir={dir}>
    <DocsSidebar sections={sections} currentSlug={currentSlug} />

    <main id="main" class="docs-content">
      {/* Locale data for header LanguagePicker — it reads this to navigate between translations */}
      <script type="application/json" id="docs-locales" set:html={JSON.stringify(availableLocales)} />

      {mdPath && (
        <div class="docs-md-link">
          <a href={mdPath} class="docs-md-btn" title="View as Markdown" data-t="docs.viewMd">View as Markdown</a>
        </div>
      )}
      <article class="docs-article prose" data-pagefind-body>
        <h1>{title}</h1>
        <slot />
      </article>
    </main>

    <TOC headings={headings} />
  </div>
  <Mermaid />
</Base>

<style is:global>
  @import '../styles/prose.css';

  .docs-layout {
    display: flex;
    max-width: 1200px;
    margin: 0 auto;
    padding: 56px 24px 0;
    gap: 40px;
  }

  .docs-content {
    flex: 1;
    min-width: 0;
    padding: 32px 0 80px;
  }

  .docs-article {
    max-width: 680px;
  }

  .docs-article h1 {
    margin-bottom: 24px;
    padding-bottom: 16px;
    border-bottom: 1px solid var(--b1);
  }

  /* MD link */
  .docs-md-link {
    display: flex;
    justify-content: flex-end;
    margin-bottom: 16px;
  }
  .docs-md-btn {
    font-family: var(--mono);
    font-size: 0.68rem;
    font-weight: 500;
    letter-spacing: 0.04em;
    color: var(--t3);
    text-decoration: none;
    padding: 4px 10px;
    border: 1px solid var(--b1);
    border-radius: 4px;
    transition: color 0.1s, border-color 0.1s;
  }
  .docs-md-btn:hover {
    color: var(--t1);
    border-color: var(--b2);
  }

  /* RTL support for docs */
  [dir="rtl"] .prose th { text-align: right; }
  [dir="rtl"] .prose { direction: rtl; }
  [dir="rtl"] .prose ul,
  [dir="rtl"] .prose ol {
    padding-left: 0;
    padding-right: 24px;
  }
  [dir="rtl"] .prose blockquote {
    border-left: none;
    border-right: 3px solid var(--ac);
    border-radius: var(--r) 0 0 var(--r);
  }

  @media (max-width: 860px) {
    .docs-layout {
      padding-top: 52px;
    }
    .docs-content {
      padding: 24px 0 60px;
    }
  }
</style>
