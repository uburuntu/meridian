{
  "name": "{% if server_name %}{{ server_name }}{% elif client_name and client_name != 'default' %}Setup — {{ client_name|capitalize }}{% else %}Setup{% endif %}",
  "short_name": "{% if server_name %}{{ server_name }}{% else %}Setup{% endif %}",
  "start_url": ".",
  "display": "standalone",
  "background_color": "#0c0e14",
  "theme_color": "#0c0e14",
  "icons": [
    {
      "src": "{{ asset_path }}/icon.svg",
      "sizes": "any",
      "type": "image/svg+xml",
      "purpose": "maskable any"
    }
  ]
}
